[project]
name = "code-template"
version = "0.2.0"
description = "Personal code template"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "cookiecutter>=2.6.0",
]

[dependency-groups]
dev = [
    "pytest>=8.3.3",
]
